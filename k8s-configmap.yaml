apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-metrics-config
  namespace: default
data:
  # Logging configuration
  LOG_LEVEL: "INFO"
  PYTHONPATH: "/app"

  # Configuration Store settings
  CONFIG_STORE_BASE_URL: "https://z-config-store.48.216.130.212.nip.io"

  # For better security, the API key should be stored as a Secret
  # But keeping it here for reference
  # CONFIG_STORE_API_KEY: "891f41ee454479c49e458c4a7c50dd1e"